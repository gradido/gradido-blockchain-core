/* CRoaring's roaring64_bitmap_t, which is what the C++ implementation uses today. */
#define BENCH_BITMAP_CROARING 1
#define BENCH_BITMAP_SYMBOL bench_bitmap_croaring64
#include "bench_bitmap_body.h"
